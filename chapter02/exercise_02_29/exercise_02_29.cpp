#include <iostream>

int
main()
{
    int number;
    
    number = 0;
    std::cout << "\nnumber\tsquare\tcube\n";
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    number = number + 1;
    std::cout << number << '\t' << number * number << '\t' << number * number * number << std::endl;
    
    
    return 0;
}
